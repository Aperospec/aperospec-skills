# Runtime 能力评估

## 1. 评估对象与结论摘要

评估对象：

`aperospec-runtime`

评估身份：

它不是创意岗位，而是你直属的 PM / 总管。

总体结论：

`Runtime` 现在已经具备了一个好 PM 最重要的骨架，尤其是在角色定位、顺序调度、上下游隔离、artifact 冻结这几个核心方向上，判断非常清楚。

但它目前更像一个原则明确的总控框架，而不是一个已经完全落地的强执行 PM。

它已经能回答：

- 谁先做
- 谁后做
- 谁不能看什么
- 哪些产物要冻结

但还没有充分回答：

- 当流程中途被打断时怎么办
- 当你临时插入新要求时怎么办
- 当某一层质量不达标时如何决定返工范围
- 当锁定内容与下游需要发生冲突时如何仲裁
- 最终渲染执行到底如何验收为“完成”

因此我的判断是：

> 它是一个方向正确、边界意识很强、但执行制度还没补全的 PM。

## 2. 诊断版

### 已经成熟的地方

#### 2.1 角色定位非常干净

`Runtime` 文档持续强调自己不是 creative skill，这一点很重要。

这意味着它没有抢夺你的创意权，也没有和 `Project`、`Cinema`、`VisualDirector` 的专业边界混在一起。

对一个 PM 来说，这个自我定位是合格的。

#### 2.2 流水线顺序非常清晰

从 `Injection` 到 `Project`、`Cinema`、`ConceptDeck`、`VisualDirector`、`Rendering Agent` 的顺序已经明确。

而且每一阶段的输入输出都被命名为独立 artifact：

`RIM -> CWP -> NWP -> CDP -> VDP -> Final Deck`

这说明 `Runtime` 已经具备项目编排意识，而不是只会“让几个 Skill 轮流说话”。

#### 2.3 边界控制意识很强

`Runtime` 最强的一点，是它非常强调 context isolation。

例如：

- `Cinema` 只能看 `CWP`
- `ConceptDeck` 只能看 `NWP`
- `VisualDirector` 只能看 `CDP`
- `Rendering Agent` 只能看 `VDP`

这对你这套系统特别重要，因为你真正怕的不是某个 Skill 不聪明，而是上下游串味。

在这一点上，`Runtime` 的方法是对的。

#### 2.4 Artifact 冻结规则已经建立

每一层产物生成后立即冻结，这是很成熟的 PM 思维。

它让返工和排错可以基于 artifact chain 来定位问题，而不是全靠主观感觉。

这也是这套系统从“创意描述”变成“可管理生产线”的关键一步。

#### 2.5 已经具备 debug 思维

`runtime-protocol.md` 已经写到：

- narrative drift
- corporate PPT style
- emotional fracture
- missing shot feeling
- broken rhythm

出现这些问题时，要先检查 artifact chain，再决定哪里失败。

这说明 `Runtime` 已经不是只会推进，还开始考虑“出了问题如何追责到具体环节”。

这是合格 PM 的重要特征。

### 主要缺口

#### 2.6 异常处理制度不完整

现在的 `Runtime` 说清楚了标准流程，但没把异常流程写成规则。

缺的主要是：

- 用户中途改方向时，如何暂停和重开
- 用户插入新资产时，是否必须重新过 `Injection`
- 某阶段产物质量不达标时，返工是从本层开始还是回溯上一层
- 锁定资产与下游生成冲突时，由谁裁决、如何裁决

这意味着它像一个“会走顺风局”的 PM，但还不是一个“遇到项目变化也稳得住”的 PM。

#### 2.7 返工边界还不够操作化

文档提到要“inspect the artifact chain”，但还没有明确返工判定规则。

例如没有写清：

- 什么问题属于 `CWP` 失败
- 什么问题属于 `NWP` 失败
- 什么问题只是 `VDP` 执行偏差
- 返工是否允许跳过中间层直接改后层

没有这些判定规则时，`Runtime` 会知道要排查，但不一定知道该怎么判责。

#### 2.8 与你的汇报机制还不够明确

`Runtime` 已经定义了对 Skill 的控制，但没有把“何时向你汇报、汇报什么”写清楚。

一个真正成熟的 PM 不只要会调度下属，也要知道：

- 什么阶段可以静默推进
- 什么阶段必须请你确认
- 哪类锁定内容必须回到你这里拍板
- 最终交付前是否必须经过你审核

目前这些规则主要靠你默认理解，还没有沉淀成制度。

#### 2.9 Stage 5 最弱

当前 `Rendering Agent` 的定义更像愿景说明，不像执行协议。

虽然它写了：

- 用 `VDP` 作为唯一 creative source
- 调用 GPT Image 2
- 插入 Hero Image
- 输出 `.pptx`

但还没写清：

- 每页失败时怎么补救
- 图像生成失败时怎么办
- 图文比例如何遵守 `VDP`
- 什么算“严格 follow VDP”
- 最终 deck 的验收标准是什么

这会直接影响 `Runtime` 对最终交付的控制力。

#### 2.10 Injection 与 Runtime 的交界仍可加强

现在 `Injection` 和 `Runtime` 的关系已经定义了，但交界还偏粗。

比如：

- `RIM` 的优先级是否永远高于标准 pipeline
- 当 `RIM` 给出 Assisted Generation 时，Runtime 要怎么把“允许补足”与“禁止改写”分开
- 当存在多个已有资产且分属不同层时，Runtime 如何合并执行

这些都是现实项目里很常见的复杂情况，但现在规则还没完全展开。

### 优先补强项

#### 第一优先级

补齐异常与返工制度。

至少要明确：

- 插单规则
- 改方向规则
- 返工起点判定规则
- 回滚与保留规则

#### 第二优先级

补齐对你的汇报与确认制度。

至少要明确：

- 哪些情况 Runtime 可直接推进
- 哪些情况必须向你请示
- 哪些内容一旦锁定必须由你确认

#### 第三优先级

把 Stage 5 从愿景描述升级成执行协议。

至少要明确：

- 渲染失败重试机制
- 图像与版式的最低验收标准
- final deck 的完成定义

## 3. 评分版

评分说明：

- 10 分：成熟且可直接稳定执行
- 7 到 8 分：框架成熟，但仍需补若干制度
- 5 到 6 分：方向正确，但执行可控性不足
- 4 分及以下：原则与执行都不稳定

### 3.1 角色清晰度

评分：

`9/10`

判断：

这是 `Runtime` 目前最强的一项。它非常明确自己不是创作岗，而是 orchestration layer。

扣分点在于：

虽然知道自己不创作，但还没完全写清与“向你汇报”的组织规则。

### 3.2 流程闭环度

评分：

`7/10`

判断：

主流程已经闭环，从上游到最终渲染都有定义。

扣分点在于：

异常流程、插单流程、返工流程还没有闭环。

### 3.3 边界控制力

评分：

`9/10`

判断：

`Runtime` 的边界意识很强，尤其是只允许阶段间传 artifact，不允许跨层看原始资料或后层内容，这一点非常成熟。

扣分点较小，主要是缺少边界冲突时的仲裁规则。

### 3.4 中间产物治理能力

评分：

`8/10`

判断：

artifact chain 与冻结规则已经建立，说明它已经具备“生产管理”思维。

扣分点在于：

虽然知道要冻结和排查，但对“何时重开哪一层”的治理制度还不够细。

### 3.5 异常与返工管理能力

评分：

`5/10`

判断：

这是当前最大短板。

`Runtime` 已经知道项目会出问题，但还没有把问题分型、返工起点、回滚范围、重新冻结逻辑写成明确规程。

### 3.6 对最终交付的可控性

评分：

`6/10`

判断：

到 `VDP` 为止控制力较强，但一到 `Rendering Agent` 就明显变松。

原因不是没有目标，而是缺执行细则与验收标准。

## 4. 总体评级

综合评分：

`7.3/10`

总体判断：

`Runtime` 已经像一个合格的上游 PM 框架，但还没有完全成为一个强执行、强异常处理、强交付控制的成熟 PM。

换句话说：

- 它已经足够定义组织秩序
- 已经足够防止大部分 creative boundary drift
- 已经足够支撑一条标准顺序流水线

但它还不够擅长：

- 应对动态项目变化
- 管理返工与插单
- 把最终交付阶段收得非常稳

## 5. 最终判断

如果问题是：

> `Runtime` 现在像不像合格 PM？

我的答案是：

> 像，而且骨架是对的。

如果问题是：

> 它是不是已经成熟到能稳定管理复杂真实项目？

我的答案是：

> 还没有完全到位。

它现在最需要的不是再强调“自己不是 creative skill”，而是把以下三件事补成制度：

1. 异常与返工规则
2. 对你的请示与确认规则
3. 最终渲染阶段的执行与验收规则

这三块一旦补齐，`Runtime` 就会从“原则正确的总控”升级成“真正能替你带队的 PM”。
